--- a/myscript.py
+++ b/myscript.py
@@
+def add(a,b): return a+b  # pretend this line was added
+eval("1+1")  # pretend this line was added
+def complex_fn(x):
+    total = 0
+    for i in range(5):
+        if x > i:
+            if i % 2 == 0:
+                total += i
+            else:
+                if x - i > 2:
+                    total += x*i
+                else:
+                    total -= i
+    return total
